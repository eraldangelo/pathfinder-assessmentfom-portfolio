rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Assessment uses Firebase Admin SDK for writes.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
